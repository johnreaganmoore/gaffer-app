<!DOCTYPE html>
<html>
  <head>
    <title>
      Let's Go
      <% if defined?(@team.name) then %>
        <%= @team.name %>
      <% end %>
    </title>
    <%= csrf_meta_tags %>

    <%= include_gon %>
    <script defer src="https://code.getmdl.io/1.1.3/material.min.js"></script>
    <script src="//maps.google.com/maps/api/js?key=<%= ENV['google_maps_geocoding'] %>"></script>
    <script src="//cdn.rawgit.com/mahnunchik/markerclustererplus/master/dist/markerclusterer.min.js"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <!-- <link rel="stylesheet" href="https://code.getmdl.io/1.1.3/material.green-blue.min.css"> -->

    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if @team != nil %>
      <% bg_color = @team.color %>
    <% else %>
      <% bg_color = "#04C860" %>
    <% end %>
    <% if @team != nil %>
      <nav>
        <div class="nav-wrapper" style="background-color: <%= bg_color %>">
          <a href="#" data-activates="slide-out" class="button-collapse"><i class="material-icons">menu</i></a>
          <span class="brand-logo">
            <% if defined?(@team.name) then %>
              <%= link_to team_path(@team) do %>
                <%= @team.name %>
              <% end %>
            <% end %>
          </span>
          <ul id="nav-mobile" class="right hide-on-med-and-down">
            <li>
              <% if person_signed_in? %>
                <%= link_to 'Sign out', destroy_person_session_path, method: :delete, class: "mdl-button mdl-js-button" %>
              <% else %>
                <%= link_to "Sign In", new_person_session_path,  class: "mdl-button mdl-js-button" %>
              <% end %>
            </li>
          </ul>
          <ul id="slide-out" class="side-nav">
            <li><div class="userView">
              <img class="background" src="images/office.jpg">
              <a href="#!user"><img class="circle" src="images/yuna.jpg"></a>
              <a href="#!name"><span class="white-text name">John Doe</span></a>
              <a href="#!email"><span class="white-text email">jdandturk@gmail.com</span></a>
              <% if defined?(@team.logo) %>
                <img src="<%= @team.logo.avatar %>" class="demo-avatar">
              <% end %>
              <% if defined?(@team.name) %>
                <%= @team.name %>
              <% end %>
            </div></li>
            <li><a href="#!"><i class="material-icons">cloud</i>First Link With Icon</a></li>
            <li>
              <a href="#!">
                <% if person_signed_in? %>
                  <%= link_to 'Sign out', destroy_person_session_path, method: :delete, class: "mdl-navigation__link" %>
                <% else %>
                  <%= link_to "Sign In", new_person_session_path,  class: "waves-effect" %>
                <% end %>
              </a>
            </li>
            <li><div class="divider"></div></li>
            <li><a class="subheader">Subheader</a></li>
            <li><a class="waves-effect" href="#!">Third Link With Waves</a></li>
          </ul>
        </div>
      </nav>
    <% else %>
      <div class="mini-header"></div>
    <% end %>
      <main class="page-content">
        <%= yield %>
      </main>
  </body>
</html>
<%= javascript_tag do %>
  window.flashMessages = $.parseJSON('<%=j flash.to_hash.to_json.html_safe %>');
<% end %>
