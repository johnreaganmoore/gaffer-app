<div class="container">

    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <div class="card-title">
          Add Members to <%= @list.name %>
        </div>
        <p>
          You can add multiple list members at once by uploading a csv below. To add individuals manually use <a style="color: #ffffff; font-weight: 700; text-decoration: underline;" href="<%= new_sub_list_membership_path(list: @list.id) %>">indvidual member creation</a>.
        </p>
        <br>
        <p>
          The first three columns in the CSV need to be these properties in this order:
          <ol>
            <li>Email</li>
            <li>First Name</li>
            <li>Last Name</li>
          </ol>
        </p>
        <br>
        <p>
          If you need help with this, or anything else, please contact us in the chat at the bottom right of the screen.
        </p>
      </div>
    </div>

    <div class="card">
      <%= form_tag list_member_batch_create_path, multipart: true, method: :post do |f| %>
        <div class="card-content">
          <div class="mdl-card__title mdl-card--expand">
            <div class="profile-header">
              <div class="file-field input-field">
                <div class="btn">
                  <span>File</span>
                  <%= file_field_tag :file, multiple: true %>


                  <!-- <input type="file" multiple> -->
                </div>
                <%= hidden_field_tag :list, @list.id %>
                <div class="file-path-wrapper">
                  <input class="file-path validate" type="text" placeholder="Upload one or more files">
                </div>
              </div>

            </div>
          </div>
        </div>
      <div class="edit-profile-actions card-action">
        <%= button_tag(type: 'submit', class: "btn waves-effect waves-light") do %>
          Import Batch
        <% end %>
      </div>
    <% end %>
  </div>
</div>
