<div class="container">

    <div class="card blue-grey darken-1">
      <div class="card-content white-text">
        <div class="card-title">
          Add Players to <%= @team.name %>
        </div>
        <p>
          You can add multiple players at once by uploading a csv below. To add individuals manually use <a style="color: #ffffff; font-weight: 700; text-decoration: underline;" href="<%= new_player_path(team: @team.id) %>">indvidual player creation</a>.
        </p>
        <br>
        <p>
          The first three columns in the CSV need to be these properties in this order:
          <ol>
            <li>Email</li>
            <li>First Name</li>
            <li>Last Name</li>
          </ol>
        </p>
        <br>
        <p>
          If you need help with this, or anything else, please contact us in the chat at the bottom right of the screen.
        </p>
      </div>
    </div>

    <div class="card">
      <div class="card-content">
        <div class="mdl-card__title mdl-card--expand">
          <div class="profile-header">

            <%= form_tag players_batch_create_path, multipart: true, method: :post do |f| %>
              <div class="file-field input-field">
                <div class="btn">
                  <span>File</span>
                  <%= file_field_tag :file, multiple: true %>


                  <!-- <input type="file" multiple> -->
                </div>
                <%= hidden_field_tag :team, @team.id %>
                <div class="file-path-wrapper">
                  <input class="file-path validate" type="text" placeholder="Upload one or more files">
                </div>
              </div>

            </div>
          </div>
        </div>
      <div class="edit-profile-actions card-action">
        <%= button_tag(type: 'submit', class: "btn waves-effect waves-light") do %>
          Import Batch
        <% end %>
      </div>
    <% end %>
  </div>
</div>
